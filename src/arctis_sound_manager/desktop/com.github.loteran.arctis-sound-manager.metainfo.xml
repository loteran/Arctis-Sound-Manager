<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>com.github.loteran.arctis-sound-manager</id>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-or-later</project_license>

  <name>Arctis Sound Manager</name>
  <summary>Linux GUI for SteelSeries Arctis headsets</summary>
  <description>
    <p>
      Arctis Sound Manager is a Linux application for configuring SteelSeries Arctis
      headsets. It provides a 4-channel audio mixer (Game / Chat / Media / HDMI),
      a full Sonar parametric EQ system, virtual 7.1 surround sound, and device management.
    </p>
    <p>Features:</p>
    <ul>
      <li>4-channel audio mixer with automatic media routing</li>
      <li>Sonar EQ — parametric EQ with 297+ presets, Spatial Audio, Smart Volume</li>
      <li>Micro processing — AI Noise Cancellation, Noise Gate, Compressor</li>
      <li>ANC / Transparent mode control from GUI and headset</li>
      <li>Virtual 7.1 surround via HeSuVi HRTF convolution</li>
      <li>10-band Custom EQ with save/load presets</li>
      <li>Device status — battery, mic mute, sidetone</li>
    </ul>
  </description>

  <launchable type="desktop-id">ArctisManager.desktop</launchable>
  <icon type="remote" width="128" height="128">https://raw.githubusercontent.com/loteran/Arctis-Sound-Manager/main/docs/images/arctis-manager-icon.png</icon>

  <url type="homepage">https://github.com/loteran/Arctis-Sound-Manager</url>
  <url type="bugtracker">https://github.com/loteran/Arctis-Sound-Manager/issues</url>

  <developer id="com.github.loteran">
    <name>loteran</name>
  </developer>

  <screenshots>
    <screenshot type="default">
      <caption>Home — 4-channel audio mixer</caption>
      <image>https://raw.githubusercontent.com/loteran/Arctis-Sound-Manager/main/docs/images/screenshot_home.png</image>
    </screenshot>
    <screenshot>
      <caption>Sonar — Parametric EQ with presets</caption>
      <image>https://raw.githubusercontent.com/loteran/Arctis-Sound-Manager/main/docs/images/screenshot_sonar.png</image>
    </screenshot>
    <screenshot>
      <caption>Equalizer — 10-band Custom EQ</caption>
      <image>https://raw.githubusercontent.com/loteran/Arctis-Sound-Manager/main/docs/images/screenshot_equalizer.png</image>
    </screenshot>
    <screenshot>
      <caption>Headset / DAC — Device info</caption>
      <image>https://raw.githubusercontent.com/loteran/Arctis-Sound-Manager/main/docs/images/screenshot_headset.png</image>
    </screenshot>
  </screenshots>

  <content_rating type="oars-1.1" />

  <releases>
    <release version="1.4.0" date="2026-08-20">
      <description>
        <p>ASM now runs without a SteelSeries headset. Asked for twice, two months apart — once with a Beyerdynamic TYGR 300R, once with a Focusrite Scarlett and an HD560S — and answered no both times, because the daemon only ever started once an Arctis replied over USB. That was true of the daemon, not of the app: the four channels, per-channel volume, the Sonar EQ, HeSuVi spatial audio, the application router and Clips never touch USB at all. They manipulate PipeWire sink names, nothing more. What genuinely needs an Arctis is the conversation with the headset — battery, ANC, sidetone, ChatMix, the OLED — and this mode does without it. Turn on *Use ASM without a SteelSeries headset* in Settings, pick the output your headphones are plugged into, and the channels are built on it. The Headset and DAC pages come up empty rather than showing controls that would do nothing, and no microphone has to be chosen if you only want playback. Nothing changes for a SteelSeries headset: the mode is off by default, it can only be reached deliberately, and a connected Arctis always wins. (#189, discussion #95). "Launch at login" stopped working after updating to 1.3.0. 1.3.0 renamed the tray unit so the desktop can attach a keyboard shortcut to it — necessary, and correct. What was missed is that the rename left the old unit behind: for anyone whose autostart pointed at the one shipped in the package, the update deleted the file it pointed to and never switched the replacement on. The tray simply stopped appearing at login, on a machine where nothing else looked wrong, and starting it by hand gave a window with no daemon behind it. The daemon now moves the setting across on its own, at the first start after the update — it restores an autostart that existed and never grants one to someone who had turned it off. Reported on two machines by @TheJurassicSnark. (#191).</p>
      </description>
    </release>
    <release version="1.3.0" date="2026-08-18">
      <description>
        <p>Clips: save the last thirty seconds, with each Sonar channel on its own audio track. Installed from Settings rather than shipped with ASM, and deliberately so: capture is the one feature that needs packages a desktop does not already have — PyGObject, four GStreamer plugin sets and ffmpeg — and an install that only wants the mixer and the equaliser should not carry a screen recorder's encoders. Settings offers an Install button that fetches exactly those packages and nothing else, and an Uninstall that turns the feature off and offers to take them away again; removal is a separate question with No as the answer it defaults to, because ffmpeg and GStreamer belong to your video player and your browser as much as to this. If a package goes missing afterwards the button says Repair rather than claiming the feature works. A rolling capture runs in the background; the Clips page — or asm-clipd bound to a key — writes the preceding seconds to ~/Videos/ASM Clips as an .mkv carrying Game, Chat and Mic as separate tracks, so what you keep can still be remixed afterwards. The library is a grid of poster frames rather than a list of timestamps that differ only by the second they were taken; the frame is lifted from five seconds before the end, inside the span the editor opens with, because the middle of a clip is usually the part where nothing has happened yet. The editor trims, remembers the span per clip, and exports to a separate Shared/ folder so an export never comes back as another card in the library. Exports are always MP4, whatever the settings: Matroska is the only container that holds a track per channel, and it is also one Discord will upload and then refuse to play, so the recording format and the sharing format are deliberately not the same thing. Exporting ends in a share step with the poster frame as a drag-out card — drag it straight into Discord. Select, rename and delete in bulk; deletion goes through the system trash, because a mis-click on a grid of near-identical cards should be recoverable. Clips: the editor's preview is a mixer — every channel plays at once, with a live level and mute on each. A media player decodes exactly one audio track and picks the first, Qt's included, so a clip whose Game channel was empty played back in total silence with three perfectly good channels sitting beside it, and the only available reading was that clips record no sound. Each channel is now given a player of its own and run in step with the picture, so the sliders and Mute do what a mixer's controls do: you hear the change while you make it, and what you hear is exactly what the export will contain. Those levels are remembered per clip, alongside the trim — deciding the microphone was too loud is a judgement about *that* recording, and reopening a clip used to make you decide it again. Channels holding nothing are marked *silent*, and if every one of them is, the editor says so and points at the Sonar routing on the Home page, which is where that is actually fixed. Clips: the editor lost its transport bar and gained a scrubbable timeline. The play button and seek slider above the preview were a second, worse copy of the trim band below them — two scrubbers for one clip, and the one that also showed the span being exported was the one worth keeping. Click the picture to play or pause (or press Space), and drag the playhead along the band to run back and forth over a moment, which was not possible at all before: dragging inside the selection moved the selection, and everywhere else was a single click.</p>
      </description>
    </release>
    <release version="1.2.34" date="2026-08-16">
      <description>
        <p>Applications could stay stuck on the headset itself, outside every channel. Anything ASM does not recognise by name, which is most things once you go past browsers and the usual chat apps, would sit directly on the headset output rather than on Game, Chat or Media: audible, but with no equaliser, no per-channel volume, no ChatMix, and absent from the mixer. ASM has a pass whose whole purpose is to bring such applications back onto a channel, and it was skipping them, because the headset's own output is named after the headset and so counted as a channel it was already on. The two are now told apart. Applications you deliberately placed on the hardware yourself stay where you put them. Reported on Discord by autune. Moving an application between channels no longer stutters. The Home page rebuilt every application tag and every row of the "other applications" list twice a second, including while you were dragging one of those widgets onto another channel. Both are now left alone when nothing about them has changed. Contributed by @Denkiyaa. (#187). The full test suite now runs on every push. Continuous integration ran a single test file out of the fourteen hundred tests in the project; everything covering routing, the mixer, the audio graph and bug reports was never executed. It caught five tests that had been failing silently since the Media channel gained its own surround chain, and it means a contributed change is now verified by the project rather than by whoever happened to run the tests locally.</p>
      </description>
    </release>
    <release version="1.2.33" date="2026-08-16">
      <description>
        <p>Channels could be completely silent on SteamOS, with everything appearing to work. The audio never reached the headset because the system refused to let ASM connect its channels to its own processing, on permission grounds. Nothing in the app looked wrong: the channels were there, the settings were saved, and a video would simply stall after a few seconds with a fraction of a second of sound. Affected users could not work around it either, since connecting the two by hand failed exactly the same way. ASM now recognises that refusal and asks the system for the one permission it needs before trying again. Reported by @baccatuesday, who ran every test that made the diagnosis possible, and confirmed by @emoeckel. Bug reports explain refused audio connections. Reports now say which program owns each part of the audio graph and what the system allowed each of them to do, which is what a refused connection turns on. asm-cli diagnose and the in-app report button now also carry the same information as each other: until now each had sections the other lacked, so which one you used decided whether your report could answer the question being asked of it.</p>
      </description>
    </release>
    <release version="1.2.32" date="2026-08-15">
      <description>
        <p>Applications playing outside the Game / Chat / Media channels are now visible, and movable. The mixer only ever listed applications that were already on one of its channels. Anything playing elsewhere — on the headset's own device rather than a channel, on an HDMI output, on a second sound card — was audible but absent from ASM, so the only way to assign it was an external tool like pwvucontrol. A new Other applications section appears under the channel cards whenever there is something to show, listing each application with the device it is currently playing on and the same one-click buttons to send it to Game, Chat, Media or Output. Applications you deliberately keep elsewhere can be dismissed from the list with the ×, and a *Show hidden* button brings them back at any time; dismissing only stops the listing, it never moves your audio. The section stays hidden when everything is already on a channel, so nothing changes for setups that were fine. Suggested by @autune, whose Nova Pro dock feeds his speakers from its AUX output — an arrangement where routing everything through a channel would be the wrong answer. Installing on Bazzite, SteamOS or Silverblue could still leave the container empty. Setting up the package repository needs a local signing key, and the installer only created one when the container's keyring looked completely empty. Container images ship with the public part already filled in and no signing key, so that step was skipped on exactly the systems it was meant for: registering the repository failed, and the install ended with "target not found" and nothing installed. The key is now always prepared. Reported on Discord by binx9612. Bug reports now state which output device is the system default. Whether an Arctis channel holds that spot decides how much of ASM applies at all, and its absence from reports has cost a round of questions with someone whose setup was perfectly deliberate.</p>
      </description>
    </release>
    <release version="1.2.31" date="2026-08-14">
      <description>
        <p>Bug reports now describe what your audio is actually doing, not just what exists. Until now a report listed your audio devices and their names, but not whether each one was playing, idle or asleep, nor what was connected to what. That is the difference between "something is keeping the headset busy" and "the surround processing is keeping it busy", and chasing the gap has cost real back-and-forth with people who took the time to report a problem. Reports now include every audio node with its live state, every connection between them, which ones are actually processing sound (with the glitch counters behind crackling), how each of ASM's own pieces is configured at that moment, and the kernel's own view of the sound card. Applications that pin their audio to a specific device show that too, which is what explains cases where another program's feature stops working. Nothing extra is asked of you: it is the same button and the same command as before, and the report stays a plain text file you can read in full before sending it.</p>
      </description>
    </release>
    <release version="1.2.30" date="2026-08-14">
      <description>
        <p>Installing on Bazzite, SteamOS or Silverblue could leave you with nothing installed. The installer created its container, then stopped partway without saying so: the application entry appeared, but opening it landed in a plain terminal where asm-gui was not found. This was a regression introduced in 1.2.27, when the installers switched to the signed package repository: if registering that repository failed for any reason (no network for the signing key, a keyring it could not sign into), the script aborted right there, after creating the container and before installing anything. Registering the repository can no longer interrupt the installation, and if the install itself fails you now get the reason and the commands to investigate instead of a silent stop. Reported on Discord by binx9612 on Bazzite. Audio from another app's virtual microphone is no longer hijacked. A stream that an application deliberately pins to its own virtual device, such as a soundboard feeding a virtual microphone, was being pulled onto an Arctis channel like any other audio. That did not merely move the sound somewhere else, it broke the other application's feature outright, and it happened to soundboards whose ordinary playback you had assigned to a channel, since both streams share one application name. Such streams are now left where their application put them, and one that had already been moved is returned. Streams pinned to real outputs are untouched, so pulling audio from other speakers onto your headset still works as before. Contributed by @nobodys-tools. (#185).</p>
      </description>
    </release>
    <release version="1.2.29" date="2026-08-12">
      <description>
        <p>ASM now tells you when the surround processing can't keep up. Spatial Audio missing its deadline sounds like random crackling, and until now nothing in the app explained where it came from — leaving you to blame the headset, the cable or the drivers. If the surround chain drops frames steadily, you get a notification saying so and pointing at the stability setting that fixes it. It deliberately stays quiet about one-off glitches: a single burst while a game loads a level or a build finishes is normal and not worth interrupting you for, so it only speaks when the problem persists for a few minutes, and only once per session. Suggested by @nobodys-tools. (#183). The Spatial Audio profile picker now shows how much CPU each profile costs. The bundled profiles range from tiny to nearly a megabyte, and the larger ones cost proportionally more processing — enough that on a busy machine the choice decides whether the audio stays clean. Each entry is now marked *light on CPU*, *moderate CPU* or *heavy on CPU*, so the trade-off is visible while you choose instead of discovered afterwards. This says nothing about sound quality: a heavier profile is not a better one, just a more demanding one. Suggested by @nobodys-tools. (#183). The Discord invite on the README and website works again. The previous invite had expired, so anyone following the "Join the Discord" badge — the first thing shown at the top of the project page — landed on an error. The replacement is set never to expire. Reported by @nobodys-tools. (#184).</p>
      </description>
    </release>
    <release version="1.2.28" date="2026-08-12">
      <description>
        <p>A stability setting for when the surround chain crackles. Spatial Audio runs fourteen convolvers per channel, and with a large HRIR that processing can miss its deadline under ordinary desktop load — heard as random crackling, with nothing in the app to explain where it comes from. Settings now offers Audio stability (buffer size): leave it on *Automatic* (the default, unchanged behaviour) or pick a larger buffer, which gives the surround processing more time per cycle and stops the glitches. Reported with measurements showing the crackling disappear at the larger size. Worth knowing before you turn it on: this is a system-wide PipeWire setting, so the extra buffering applies to every application, not only ASM — comfortable for music and films, less so for competitive play, which is why it is off by default and left to you. ASM releases it when it exits, and restores your choice when it starts. Suggested by @nobodys-tools. (#183). The app kept scanning your audio system after you closed its window. The Channels page started a twice-a-second refresh when it was first shown and never stopped it, so once the main window had been opened the work carried on for as long as the tray icon was there — with nothing on screen to show for it. Each pass inspected the whole PipeWire graph, which on a busy system was enough to keep a core noticeably occupied and to disturb the Spatial Audio processing, heard as crackling. The refresh now runs only while the page is actually visible, and the expensive part of it runs less often, so nothing changes on screen. Found and diagnosed precisely by @nobodys-tools. (#182).</p>
      </description>
    </release>
    <release version="1.2.27" date="2026-08-12">
      <description>
        <p>Provisional support for an Arctis 7+ hardware revision (0x220c). A dongle reporting this product id was in no device profile, so ASM recognised nothing and sent it nothing at all — no settings, no battery, no ChatMix. Audio worked throughout, because that side is plain USB audio handled by the kernel, which makes the gap easy to miss: everything sounds fine while nothing you set in the app reaches the headset. It also meant no udev rule was generated for it, so the device was denied access even after running asm-setup. This revision exposes a different USB layout from the other 7+ variants (HID interfaces 0 and 1, where they use interface 3), so it gets its own profile rather than a fifth product id on the existing one. Marked provisional: the control interface and command set are inferred from the endpoint sizes and the rest of the family, and not yet confirmed on hardware — if your 7+ behaves oddly rather than simply working, please say so in the issue. Reported by @Michsior14. (#180). The Channels tab's per-channel output device now actually sticks. Sending a channel to something other than the headset — Media to your speakers, say — appeared to do nothing for any app that also had a saved routing override on that channel. The setting was saved correctly and the move itself worked; two enforcement passes then fought over the stream every few seconds. The per-app override pass resolved to the bare virtual sink and moved the stream back to the headset, the channel-output pass moved it out to the speakers again, forever. Both passes now resolve through the same destination before acting, so the pick holds — and when the configured output isn't currently plugged in, the channel falls back to the headset instead of routing to a sink that doesn't exist. Contributed by @relxek. (#177). Arch and CachyOS were stranded on 1.2.25. The v1.2.26 repository publish lost its database upload to a transient HTTP 503 from GitHub. The packages went up, the index did not — and pacman resolves against that index, so the signed repository kept advertising the previous version to everyone while the new package sat there unreferenced. Uploads are now retried, and the database is published *after* the packages it names, so an interrupted run leaves the previous coherent repository rather than an index pointing at missing files. The release delivery check was also looking the wrong way: it verified the AUR — which nothing on this side can guarantee, since pushing depends on aur.archlinux.org's git being reachable — while never checking the repository Arch users install from. It now reads the actual pacman database, and an AUR outage no longer fails a release or buries a real problem under recurring noise. (#178, #176).</p>
      </description>
    </release>
    <release version="1.2.26" date="2026-08-10">
      <description>
        <p>A second microphone noise-cancellation engine: DeepFilterNet. Next to the existing RNNoise (ClearCast), the mic noise-cancel card now lets you choose DeepFilterNet — a deep-learning suppressor many find cleaner across different microphones. It isn't packaged by distributions, so if you switch to it (or turn noise cancellation on) without the plugin installed, ASM offers to download the prebuilt LADSPA plugin with one click, into ~/.ladspa; if you install a newer version yourself later, ASM uses that instead. Works on glibc x86_64/aarch64/armv7 Linux, including immutable distros via the bind-mounted ~/.ladspa. Suggested by the community. SteamOS: the app crashed on launch, and couldn't reinstall its dependencies, after a system update. A SteamOS update replaces the whole root filesystem — which wipes the pacman-installed ASM package and its Python dependencies (like pulsectl) — and pacman can't write them back because the filesystem is read-only. Three separate problems came out of that, all fixed. The GUI no longer crashes when pulsectl is missing: it opens with external-output routing disabled instead of dying on an unguarded import. The in-app dependency installer now installs pure-Python modules (pulsectl, dbus-next, ruamel.yaml, pyusb, Pillow) with pip install --user into your home directory — root-less, and surviving the next OS update — so its Install button actually resolves them on SteamOS, and the copied command no longer carries a pointless sudo. And pacman -S arctis-sound-manager from the signed repository now completes, where before it aborted on noise-suppression-for-voice — an AUR-only plugin that was wrongly a hard dependency and wasn't in the repo; it's now optional (the mic noise-cancel toggle degrades gracefully without it, and DeepFilterNet is an alternative) and shipped in the repo for those who want it. Reported by @baccatuesday. (#175).</p>
      </description>
    </release>
    <release version="1.2.25" date="2026-08-09">
      <description>
        <p>The DAC's custom display now updates as you change its settings. Turning display elements on or off, changing their fonts or scroll speed, or editing the weather only *saved* the setting — the screen was redrawn just for the brightness slider and the Custom Display on/off switch, so every other change appeared only after toggling Custom Display off and on again. Those settings now apply live, the moment you change them — no toggle, and no Apply button needed. Reported by @jnunez2301. (#172). Two copies of the app could open at login. The single-instance guard waited briefly for a reply from an already-running instance and, on silence, concluded its socket was stale, deleted it and started a second full GUI — but a freshly-launched instance accepts connections for a few seconds before it finishes starting and can reply, and login (the systemd unit and the desktop entry a moment apart) lands squarely in that window. It now settles the question on the connection being *accepted* rather than answered, and never unlinks a socket that is still live, so exactly one instance runs. Contributed by @Denkiyaa. Clicking the tray icon could crash the whole app. The click arrived as a D-Bus call the tray host was still waiting on, and the handler built the main window inline; Qt processed events underneath it, a status update carrying no battery level hid the battery tray item, and hiding it destroyed the object whose click handler was still on the stack — a use-after-free that took the whole app down, which read as the app closing itself. The click now opens the window on the event loop instead of inline. Contributed by @Denkiyaa.</p>
      </description>
    </release>
    <release version="1.2.24" date="2026-08-08">
      <description>
        <p>The Sonar equaliser now applies as you edit it — no Apply button, and no drop in the audio. Adding, removing or muting a band used to regenerate the channel's filter-chain and restart it, which tore down every EQ node and silenced the channel for several seconds; only dragging an existing band applied live. Bands now sit in a fixed-shape rack, so every edit — add, delete, mute, move — is a control change pushed straight into the running graph in tens of milliseconds instead of a restart. The Apply button is gone with the cost that justified it; Revert stays, and now returns to the curve the preset holds. Contributed by @Denkiyaa. (#170). The chat EQ could lose the output it was pointed at. The GUI regenerates a channel's filter-chain config on every EQ edit, but only the daemon knows which headset is attached — so the GUI wrote the chat channel's target out empty, and PipeWire's session manager then tried to route that output to the default sink (one of ASM's own loopbacks), failed on the resulting cycle, and retried on every graph change: 23 failed link attempts in three minutes on the machine it was caught on, audible as interruptions on whatever was actually playing. The GUI now keeps whatever target is already on disk when it can't resolve one itself, and the daemon repairs a config that was already written without one. Contributed by @Denkiyaa. (#170). A headset added by an update could still be refused USB access. If ~/.config/arctis_manager/devices/ held an old copy of a device profile — the pre-2026 asm-setup wrote one there on first run — it shadowed the packaged profile when the udev rules were generated, so a product id added in a later release (for example the Arctis Nova 7P Gen 2's 0x2298) never reached the rules file even after upgrading, and the headset was denied access. The rules generator now unions the product ids across your copy and the packaged profile, so a stale copy can no longer hide a newer PID — and a product id you add by hand is granted access too. Reported by @camperotactico. (#146).</p>
      </description>
    </release>
    <release version="1.2.23" date="2026-08-06">
      <description>
        <p>**Auto mic-switch can now trigger on disconnect *or* mute together. The microphone-switching feature added in 1.2.22 let you pick one trigger — headset disconnects, or headset mic muted. There is now a combined option: switch to your alternate microphone whenever the headset is either powered off or** muted, and back to the headset mic only once it is both connected and unmuted. Handy if you want the desk mic to take over the moment you step away *or* mute, without choosing between the two. The Spatial Audio Immersion and Distance sliders now actually do something — on both Game and Media. Moving a slider only ever wrote the new value to a settings file; the virtual-surround chain was never rebuilt, so the sound never changed unless you deleted the config by hand and restarted the daemon. The reason was subtle: the slider handler asked to regenerate the chain from the GUI process, which — unlike the background daemon — has no headset registered, so the request quietly did nothing. The daemon now rebuilds the chain when a slider moves and applies it live. On top of that, Game and Media each get their own independent Immersion/Distance instead of sharing one chain fed only by the Game values, so you can run, say, more reverb on Game and none on Media. Moving a slider briefly restarts the surround chain (a short audio glitch), which is the cost of applying it. Reported with a precise root-cause analysis by @cookiekiller. (#169). Nova Pro Wireless / GameDAC 2: the volume and ChatMix dial responds immediately again. Turning the dial only registered every few seconds, because the daemon's endpoint listener treated a normal "nothing to read yet" timeout as a fault and backed off for a full second before listening again — so most of the dial's rapid updates arrived during the back-off and were dropped. A routine read timeout is no longer treated as an error, so dial movements come through as you make them. Reported on Discord.</p>
      </description>
    </release>
    <release version="1.2.22" date="2026-08-04">
      <description>
        <p>Switch the microphone the Sonar Micro EQ uses between your headset and a desktop mic — by hand or automatically. Picking the input by hand was already possible; this adds doing it on an event. Choose an alternate microphone and a trigger: switch to it when the headset disconnects or powers off (and back to the headset mic when it returns), or when you mute the headset mic (and back when you unmute). The change happens behind the Sonar Micro EQ, so an app — a Discord call — never sees its microphone disappear; it keeps recording from the same node while the source underneath it swaps. Manual switching works on every headset; the automatic triggers act on whatever a given headset actually reports, so one that never announces its mute state simply won't use that trigger rather than misbehaving. The update button no longer runs a command that can't do anything. When ASM was installed from a package you downloaded by hand — a .deb, .rpm or .pkg that no repository tracks — the in-app update ran your package manager's upgrade command, which found nothing to fetch, reported success, and left the version unchanged while the "update available" banner kept coming back. ASM now notices that case up front (on apt, dnf and pacman alike) and instead offers the one-line command to add its repository — after which updates apply normally — or a button to download the latest release. (#163).</p>
      </description>
    </release>
    <release version="1.2.21" date="2026-08-03">
      <description>
        <p>Random audio dropouts on a healthy setup are gone. ASM reads the PipeWire graph with pw-dump, and once in a while — when a node appears or disappears at the exact moment it runs — pw-dump prints a second, tiny JSON fragment stuck onto the end of the real one. ASM took that as a parse error and concluded the entire graph had vanished, so its watchdog tore down and rebuilt the loopbacks and restarted the filter-chain — audible as a dropout every 15–25 minutes on hardware that was working perfectly the whole time. The graph is now read back even when pw-dump doubles up its output. Diagnosed on a Nova Pro Wireless by @relxek. (#164). Ubuntu 26.04: the app now starts after a clean install. On 26.04, PySide6 is split into separate per-module packages, and ASM's dependency on them was written in a way (… | python3-pip) that let the package manager consider it satisfied by python3-pip — which nearly everyone already has — and skip the actual Qt modules. asm-gui then died at launch with No module named 'PySide6.QtNetwork'. The Qt modules are now hard dependencies, so a plain install pulls them in. Traced to the exact missing submodules by @n6dlh. (#163).</p>
      </description>
    </release>
    <release version="1.2.20" date="2026-08-02">
      <description>
        <p>Stream Guard: screen sharing no longer broadcasts your whole system. Discord's Linux client has no way to pick which audio a screen share carries. When a share starts it creates its own capture node and links *every* playback stream into it — the browser tab you left open, your music, the voice call itself — and it keeps relinking, so unlinking by hand does not hold. A new bar at the top of the Sonar page picks which channels are allowed out: leave it on Game and only the game goes to the call, while Media and Chat stay in your headset. The asm-stream-guard service enforces the choice on PipeWire graph events rather than on a timer, so a stream Discord has just relinked is cut in tens of milliseconds instead of on the next poll — the gap during which private audio is already on the wire. Sources are matched by the channel they play on, not by application name, so anything you move to an allowed channel is included automatically and nothing needs to be kept in a list. The microphone chain could be fed by the headset's own output — and other people heard it. On an Arctis Nova 7 sitting on an iec958-stereo profile the headset exposes no capture node of its own, and the microphone slot fell back to the *output* sink. That sink's monitor was then linked into the microphone EQ, so everything audible on the machine — a browser tab, the game — was transmitted as if it were the microphone: a video played in a browser went out over a Discord call as though the user were speaking it. It came back on every daemon restart, which is why it reappeared with each update. The input no longer inherits the output's fallback, and the capture link is now refused outright when the proposed source is a sink. This is the one input-side mistake that is completely inaudible to the person making it. Arctis Nova Pro Omni: the microphone sidetone could not be turned off. The command that sets it kept its on/off byte pinned to on, so the slider's lowest position was a quiet sidetone rather than silence — and it switched itself back on at every reconnect no matter what you chose. The slider now reaches a true off, and off is the default. (#161).</p>
      </description>
    </release>
    <release version="1.2.19" date="2026-07-30">
      <description>
        <p>Profile fixes now reach you even if you installed ASM months ago. This one was silently cancelling out a lot of the work in recent releases. At first launch, ASM copied every device profile into your own configuration folder — and that copy takes precedence over the packaged one, forever, because no update ever refreshed it. So every profile fix shipped after your first run was inert for you: new headsets ASM had just learned to drive, corrected status byte offsets, fixed EQ commands, none of it landed. You would update, see the new version number, and keep running the protocol of the release you first installed. If you have been reporting a problem that "still isn't fixed" after several updates, this is very likely why. The daemon now reconciles that folder at every start: a copy identical to the packaged profile is removed, one you modified is moved aside to devices-disabled/ and named in the log so nothing of yours is destroyed, and a profile for a headset ASM doesn't ship is left strictly alone. Arctis Nova 7 / 7P Gen 2, Nova 5, Nova 3 Wireless, GameBuds: the custom EQ sliders did nothing at all. Every equaliser frame went out one byte off, so the headset read an opcode of zero and discarded the curve without a word. Confirmed on hardware: a Nova 7P Gen 2 reported its untouched factory preset while a custom curve was set in the app. The readback decoder was misreading its answer by one byte too, and both are now pinned against real replies from the headset. Arctis Nova Pro Wireless and Nova Pro Wired GameDAC: a custom EQ you could see and not hear. The base station drew your curve on its screen as you moved the sliders — proof the command was arriving — while the sound never changed, because the headset kept applying its Flat preset and nothing ever switched away from it. The curve is now selected once it has been written. If you never touch these sliders, nothing changes: the headset stays neutral behind the Sonar EQ exactly as before.</p>
      </description>
    </release>
    <release version="1.2.18" date="2026-07-29">
      <description>
        <p>Custom EQ: your saved curve now reaches the headset at startup. On the Nova 7 / 7P Gen 2, Nova 5, Nova 3 Wireless and GameBuds, the curve stored in the app was replayed at every start in a format only the Nova Pro Wireless understands, so the headset discarded it without a word. The custom EQ was effectively inert after each restart and only came alive if you happened to move a slider by hand. Arctis Nova 3P / 3X Wireless: the ChatMix dial should now report to the app. Turning the wheel produced no USB traffic whatsoever — it was acting as a plain local volume knob. On this dongle ChatMix is a mode the computer has to switch on, and ASM never did; it does now. (If your dial still does nothing after this update, please say so in #149 — this one could not be tested on hardware here.). Bluetooth indicator no longer shows a state the headset never sent. On the Nova 7 Gen 2, Nova 7P Gen 2 and Nova 5, the connection indicator read a byte SteelSeries' own specification marks as unused, so what it displayed was padding dressed up as a link state — on the Nova 5, a headset that has no Bluetooth at all. The row is gone rather than wrong.</p>
      </description>
    </release>
    <release version="1.2.17" date="2026-07-28">
      <description>
        <p>Sonar equaliser: delete a point with a keypress, quick-save, and revert. From user feedback (#152). Select a point on the EQ curve and press Del (or Backspace) to remove it, alongside the existing double-click. The preset Save button now overwrites the active preset directly — updating a custom preset is one click, no retyping its name — while a new Save as… button creates a new one. And a Revert button appears next to Apply whenever there are unapplied curve edits, so a set of changes can be dropped back to the last-applied state without hunting for the previous values. Arctis Nova 3P / 3X Wireless: the microphone mute state now shows in the app. Pressing mute did nothing visible, because the headset sends that event (and its battery, connection and charging pushes) on a USB interface ASM wasn't listening to — a raw HID capture from @rhavinx pinned it down. ASM now listens on that interface too, so the Microphone section tracks mute live, and battery/connection/charging update the moment they change instead of only on the next poll. (ChatMix on the dial is still being investigated — on this hardware the wheel doesn't report a mix level over USB at all, which is a different problem from the mute one.).</p>
      </description>
    </release>
    <release version="1.2.16" date="2026-07-27">
      <description>
        <p>Arctis Nova 3P / 3X Wireless: the ChatMix dial and the mic-mute button now do something in the app. The new profile shipped in 1.2.14 read both of the headset's live events one byte too far, so turning the dial never moved the Game/Chat balance — it felt like plain system volume — and pressing mute was never reflected anywhere, with the Microphone section staying blank. Both are read from the right place now: the dial moves the Game/Chat sliders, and the mute state shows in the app. As a side effect the profile no longer forces Sonar Game volume down to a sliver on start-up. Thanks to @rhavinx, who captured the real hardware traffic that pinned it down. Battery, charging and connection state — already working — are unchanged.</p>
      </description>
    </release>
    <release version="1.2.15" date="2026-07-27">
      <description>
        <p>An update now actually takes effect, instead of waiting for your next reboot. A package manager replaces files on disk; it does not touch what is already running. ASM went on executing the version it had loaded at startup — so someone who updated *because* of a fix still saw the bug, and reported it against a version number they were not running. The background services are now restarted at the end of the update, on Arch, Fedora and Debian alike. The window, which your desktop starts rather than the system, notices on its own and offers a restart: "Version X is installed — restart ASM to run it". Nothing of the sort appears for anyone running from a source checkout. The in-app update no longer insists on an AUR helper on Arch, CachyOS and derivatives. It proposed paru -S whatever the origin of the package; installed from a repository, a plain pacman -Syu does the job and your system's own updater picks it up unprompted. ASM now asks pacman which of the two situations it is in. ASM can be installed from a signed binary repository on Arch, CachyOS, EndeavourOS and other Arch derivatives — no AUR helper, no compilation, and updates arriving with the rest of the system. Packages and database are signed with the project's key, and the repository is rebuilt on every release.</p>
      </description>
    </release>
    <release version="1.2.14" date="2026-07-27">
      <description>
        <p>The in-app update no longer reports success while updating nothing, on Fedora, Nobara and Ultramarine. ASM checked for its system package by name — and on those distributions the project is redistributed under a different one, python3-arctis-sound-manager. So the check concluded there was no system package at all, fell through, and offered a *pip* update to someone running one; and even once found, the upgrade command named the package too, so the update ran, announced success, and left the version untouched. Both now ask the package manager which package owns the running code, whatever a repackager chose to call it. (#146, discussion #140). The custom equaliser button is back on the headsets it was written for. 1.2.11 brought the on-device equaliser to nine models, but the switch stayed hidden on most of them: the daemon decided whether a headset had one by looking for a single command, and the families using a parametric format don't declare one. The Nova 7 Gen 1 and Gen 2, Nova 7P, Nova 5, GameBuds and Nova 3 Wireless all lost the control they had just gained — only the two families using the older plain-gain format kept it. Reported by @camperotactico, who asked whether his Nova 7P Gen 2 supported the feature at all. It does. (#146). The fourth audio channel now carries your headset's name, like the other three. In a system audio picker you saw "*headset* Game", "*headset* Chat", "*headset* Media" — and "Sonar Output EQ", which reads as an unrelated technical entry rather than the output of the same device. Reported by @camperotactico, who counted seven headset-related outputs and could not tell which was which. (#146).</p>
      </description>
    </release>
    <release version="1.2.13" date="2026-07-26">
      <description>
        <p>The Arctis Nova 3P / 3X Wireless now report their state as it changes. The profile released yesterday only polled the headset for its status; this family relies on the dongle announcing changes instead — SteelSeries' own documentation calls the connection event "crucial for Nova 3 wireless devices to ensure accurate data retrieval". ASM now listens to all five: ChatMix, microphone mute, battery level, connection state and charging state. In practice that means the battery and the charging indicator actually follow the headset, ChatMix moves with the dial, and the mute button is reflected in the app. Found by comparing notes with @rhavinx, who had reverse-engineered the same headset from USB captures (#149) — his captures caught three frames the documentation alone had not led me to.</p>
      </description>
    </release>
    <release version="1.2.12" date="2026-07-26">
      <description>
        <p>The Arctis Nova 3P and 3X Wireless have their own profile at last. They were sharing one with the *wired* Nova 3 and being driven as if they were that model — a different report id, a different transport, and on a USB interface whose endpoint is physically too small to carry their commands. Which is why several of their controls did nothing, and why they had no battery reading at all. Owners now get the battery level and powered on/off detection — and with it the automatic audio switching that depends on knowing whether the headset is on — plus ChatMix, a sidetone with ten steps where the shared profile offered four, the volume limiter, the sleep timer, the range/quality switch, and the on-device equaliser. One control disappears: the microphone LED brightness, which these dongles have no command for — ASM was sending it into the void. Huge thanks to @rhavinx (#149), whose hardware listing supplied the one detail SteelSeries' protocol documentation does not contain.</p>
      </description>
    </release>
    <release version="1.2.11" date="2026-07-26">
      <description>
        <p>Your headset's own equaliser is now used, on nine models. Until now the custom EQ was applied by the computer, and only ever reached the hardware on the Nova Pro Wireless and Wired — on every other model it did nothing at all. The curve is now set inside the headset on the Nova 7 Gen 1 and Gen 2, Nova 7P, Nova 5, Nova 4 and Arctis GameBuds as well. That means it follows the headset to a console, a phone or another computer, costs no CPU, and stays active even when ASM isn't running. Each family speaks a different dialect for this — different opcodes, different frame layouts, some needing a pause between writes — all of which is now handled per model. Not yet verified on hardware for the newly added families: if your equaliser sounds wrong rather than absent, please say so in #146. The Nova Pro Omni and Nova Pro Wireless expose their Bluetooth preferences: whether Bluetooth comes up with the base station, and what an incoming call does to game audio (nothing, lower by 12 dB, or mute). Both were previously forced to a fixed value at every start, silently undoing whatever you had set from the DAC itself or from SteelSeries GG. The Nova Pro Omni's microphone noise reduction — done inside the base station, rather than by a plugin on your computer.</p>
      </description>
    </release>
    <release version="1.2.10" date="2026-07-26">
      <description>
        <p>The Arctis Nova 4 and Nova 4X are now supported. A whole family ASM simply did not recognise. You get the battery level, powered on/off detection and the audio routing that follows from it, sidetone, microphone volume, mute-LED brightness, the volume limiter and the sleep timer. Its battery is reported in five steps rather than as a percentage, which is how the headset itself works. If ASM detects your Nova 4 but no control does anything, please say so in #149 — one command's output is all that's needed to finish the job. The Arctis Nova 7X White and the Arctis Nova Pro Wired V2 are now recognised. Both speak a protocol ASM already implements and has had validated by users; only their product ids were missing, so the headsets were simply not detected. The charging indicator no longer disappears when your headset finishes charging. The headset reports four charging states — unknown, charging, plugged in but not charging, and running on battery — and ASM only understood two of them. The other two showed nothing at all, which meant the indicator went blank at the two moments you are most likely to look at it: once the battery reached full on the cable, and while the headset was off. Fixed on the Arctis 7+, Nova 5, and all four Nova 7 families. (Arctis 7+ owners: "on battery" was previously indistinguishable from "not charging".).</p>
      </description>
    </release>
    <release version="1.2.9" date="2026-07-26">
      <description>
        <p>The Arctis Nova Pro Omni's controls finally work. Its profile was a Nova Pro Wireless copy, and the DAC quietly refused almost everything ASM sent it: commands went out with the wrong HID report id, so the base station dropped them without a word — which is why sidetone, the volume limiter and the DAC screen looked dead however you set them. Huge thanks to @joorklee, who reverse-engineered the real protocol on hardware (a Windows capture of SteelSeries GG driving the DAC, GG's own device specification, and Linux writes confirmed by reading the values back off the base station) and shared everything in #147. Sidetone, the volume limiter, ANC and its level, transparency, mic volume and LED, the OLED screen and the auto-off timer now speak the language this DAC actually understands, and the headset's battery, charging state and Bluetooth status are reported as the device really sends them instead of being guessed from a sibling model's layout. The base station also pushes changes as they happen, so what you do on the DAC shows up in ASM without waiting for the next poll. (#70, #147, protocol by @joorklee). Arctis Nova 7P Gen 2 (1038:2298) is now recognised out of the box. (#146, reported by @camperotactico). New Sonar presets show up the day they are published. The check ran at most once every 24 hours, so a preset pack released shortly after your last check stayed invisible for a full day and reopening the app changed nothing. ASM now looks for new presets on every launch.</p>
      </description>
    </release>
    <release version="1.2.8" date="2026-07-22">
      <description>
        <p>The Output channel now has a device selector in the Channels tab, like Game, Chat and Media. Its destination could previously only be changed from the Settings page. Both places stay in sync, and your headset appears in the list. (#139). The headset can now be selected as the Output channel's device. It was deliberately hidden from the external-output list, on the assumption that it already had its own routing. That prevented a legitimate setup: sending the Output channel to the headset gives you a second path to it with its own equaliser — typically flat — and no spatial processing, which is what you want for video editing or music production without swapping EQ profiles every time. The headset now appears in the list alongside your other outputs; ASM still never picks it automatically, so nothing changes unless you choose it. (#139, requested by @Malev0). ASM could vanish from the app launcher after an update. Its launcher entry is installed by the package, and setup skipped writing its own copy whenever that one was present — so when a package update left the system file missing, nothing restored it and the app disappeared from the menu even though it still ran fine. ASM now always writes its own entry, which takes precedence over the packaged one without duplicating it, and removes it again when you uninstall. (reported on Discord by @林莫英, Fedora/COPR).</p>
      </description>
    </release>
    <release version="1.2.7" date="2026-07-21">
      <description>
        <p>Arctis_Game / Arctis_Chat / Arctis_Media could appear duplicated in the mixer, with one copy stuck silent, after a daemon crash or a forceful systemctl restart. Each virtual channel is a pw-loopback process kept alive across brief hiccups with node.linger=true; when the daemon itself died without going through a clean shutdown, those processes survived it as orphans the next daemon instance had no record of, so it started three brand new ones with the exact same names right alongside the old ones — PipeWire accepts the duplicates without complaint. The daemon now sweeps for and terminates any leftover loopback claiming a channel's name before starting its own, so a restart always comes back to a clean set of three, however the previous instance went down. Apps routed to the "Output" channel could play into silence. The last hop of that channel — its equaliser output into the external sink (HDMI, TV, speakers) — was the only one in ASM that belonged to nobody: it was not covered by the link ownership applied to Game and Media, nor by the watchdog that re-establishes the Chat and surround outputs. Every filter-chain restart tore that link down and nothing ever put it back, so an application pinned to the Output channel kept playing into a dead end, with no sound and no error message. The watchdog now enforces this hop too, following the external output you selected. Releasing the headset no longer leaves USB interfaces in limbo. ASM claims every USB interface it uses when it takes the device over, but on teardown it released only one of them before handing the device back to the kernel driver — which cannot take back an interface still held by another process. Should that happen while switching between two different headset models, the affected interfaces ended up owned by neither ASM nor the kernel until the device was replugged. Claim and release now cover the same set.</p>
      </description>
    </release>
  </releases>

  <categories>
    <category>Audio</category>
    <category>Utility</category>
  </categories>

  <provides>
    <binary>asm-gui</binary>
  </provides>
</component>
