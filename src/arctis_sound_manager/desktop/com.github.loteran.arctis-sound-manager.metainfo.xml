<?xml version="1.0" encoding="UTF-8"?>
<component type="desktop-application">
  <id>com.github.loteran.arctis-sound-manager</id>
  <metadata_license>CC0-1.0</metadata_license>
  <project_license>GPL-3.0-or-later</project_license>

  <name>Arctis Sound Manager</name>
  <summary>Linux GUI for SteelSeries Arctis headsets</summary>
  <description>
    <p>
      Arctis Sound Manager is a Linux application for configuring SteelSeries Arctis
      headsets. It provides a 4-channel audio mixer (Game / Chat / Media / HDMI),
      a full Sonar parametric EQ system, virtual 7.1 surround sound, and device management.
    </p>
    <p>Features:</p>
    <ul>
      <li>4-channel audio mixer with automatic media routing</li>
      <li>Sonar EQ — parametric EQ with 297+ presets, Spatial Audio, Smart Volume</li>
      <li>Micro processing — AI Noise Cancellation, Noise Gate, Compressor</li>
      <li>ANC / Transparent mode control from GUI and headset</li>
      <li>Virtual 7.1 surround via HeSuVi HRTF convolution</li>
      <li>10-band Custom EQ with save/load presets</li>
      <li>Device status — battery, mic mute, sidetone</li>
    </ul>
  </description>

  <launchable type="desktop-id">ArctisManager.desktop</launchable>
  <icon type="remote" width="128" height="128">https://raw.githubusercontent.com/loteran/Arctis-Sound-Manager/main/docs/images/arctis-manager-icon.png</icon>

  <url type="homepage">https://github.com/loteran/Arctis-Sound-Manager</url>
  <url type="bugtracker">https://github.com/loteran/Arctis-Sound-Manager/issues</url>

  <developer id="com.github.loteran">
    <name>loteran</name>
  </developer>

  <screenshots>
    <screenshot type="default">
      <caption>Home — 4-channel audio mixer</caption>
      <image>https://raw.githubusercontent.com/loteran/Arctis-Sound-Manager/main/docs/images/screenshot_home.png</image>
    </screenshot>
    <screenshot>
      <caption>Sonar — Parametric EQ with presets</caption>
      <image>https://raw.githubusercontent.com/loteran/Arctis-Sound-Manager/main/docs/images/screenshot_sonar.png</image>
    </screenshot>
    <screenshot>
      <caption>Equalizer — 10-band Custom EQ</caption>
      <image>https://raw.githubusercontent.com/loteran/Arctis-Sound-Manager/main/docs/images/screenshot_equalizer.png</image>
    </screenshot>
    <screenshot>
      <caption>Headset / DAC — Device info</caption>
      <image>https://raw.githubusercontent.com/loteran/Arctis-Sound-Manager/main/docs/images/screenshot_headset.png</image>
    </screenshot>
  </screenshots>

  <content_rating type="oars-1.1" />

  <releases>
    <release version="1.1.53" date="2026-06-02">
      <description>
        <p>Left-click the tray icon to open the window — a single left-click on the Arctis Sound Manager system-tray icon now opens the main window (launching it the first time, raising and focusing it afterwards). Previously the window could only be reached through the tray's right-click context menu. Right-click still shows the context menu.</p>
      </description>
    </release>
    <release version="1.1.52" date="2026-06-02">
      <description>
        <p>Microphone filter-chain crashed PipeWire when ClearCast AI noise cancellation was enabled (#69) — the rnnoise LADSPA plugin (librnnoise_ladspa) aborts when it is fed buffers at a sample rate it was not built for, taking down the whole filter-chain service (pipewire -c filter-chain.conf) and leaving the system with no audio until reboot. The mic filter-chain is now pinned to 48 kHz — the rate rnnoise expects — so the plugin always receives valid buffers. Periodic ~1 s microphone dropouts in apps using echo cancellation (#68) — Discord/Teams/Steam WebRTC echo cancellation drifted against the virtual mic source's clock and reset roughly once per second, silencing the mic for a split second at a steady cadence. Pinning the mic filter-chain to 48 kHz and locking its quantum (node.lock-quantum) removes the resampling and clock drift, so app-side echo cancellation can stay enabled without stuttering.</p>
      </description>
    </release>
    <release version="1.1.51" date="2026-06-02">
      <description>
        <p>Arctis 7+ reported as offline while actually connected — the power-status parser only recognised a single "online" byte (0x02), but the Arctis 7+ firmware reports several online sub-states (0x00, 0x02, 0x03). When the headset was in any of the unmatched states it was treated as powered off, so online detection and audio redirection failed. The parser now enumerates all online states explicitly via int_str_mapping (thanks @Michsior14 — #67). Bumped pygments to 2.20.0 (fixes a low-severity ReDoS advisory in the GUID regex) and pytest to 9.0.3 (dev dependency).</p>
      </description>
    </release>
    <release version="1.1.50" date="2026-06-02">
      <description>
        <p>Arctis Nova 5X (PID 0x2255) support — the 0x2255 variant is now declared in the device YAML, so it is detected and managed like the other Nova 5/5X models. Its udev rule is generated automatically at build time. Unmapped PIDs are surfaced in red in the README device table — any Product ID reported by telemetry but not yet declared in a device YAML is now listed automatically in red, so it can be triaged and promoted into a YAML. The list self-cleans: a PID drops out once it lands in a YAML. The device table also gained an lsusb -d 1038: snippet so users can find their own Product ID.</p>
      </description>
    </release>
    <release version="1.1.49" date="2026-06-01">
      <description>
        <p>RNNoise mic noise-suppression dependency failed to install on Ubuntu (#65) — the plugin is not packaged for Ubuntu or its derivatives (Linux Mint, Pop!_OS, elementary, KDE neon), yet ASM suggested an apt package that has no candidate there. Those distros now build the LADSPA plugin from source in one click (Install / Copy cmd), and the dependency is no longer treated as blocking — the rest of ASM works without it. Debian keeps the apt package; Fedora and Arch are unchanged. The "Copy cmd" button also now preserves command quoting correctly. On the DAC OLED, the EQ-mode badge (S = Sonar, C = Custom EQ) is now shown to the right of the clock instead of on its own line.</p>
      </description>
    </release>
    <release version="1.1.48" date="2026-05-31">
      <description>
        <p>Microphone Background / Impact noise reduction could not be turned off — same Qt.CheckState truthiness bug as the Spatial Audio toggle in 1.1.47; the OFF state is now registered correctly. GUI could crash (SIGABRT) on a quick EQ-mode toggle or when applying a preset from the tray — the background apply thread could be destroyed while still running. Thread cleanup now waits for the thread to fully stop (same class as the Smart Volume slider crash fixed in 1.1.47). The daemon froze for up to several seconds while reloading its configuration (e.g. on ReloadConfigs / loopback recreation) because blocking device discovery ran on the asyncio event loop. That work is now offloaded to a worker thread, keeping the daemon responsive.</p>
      </description>
    </release>
    <release version="1.1.47" date="2026-05-31">
      <description>
        <p>App audio channel assignments were "randomly forgotten" (#64) — moving an app to a channel (e.g. LibreWolf → Media) was only persisted for apps the router was already tracking; an app sitting on the default channel was never recorded, so the manual move was never saved and the app kept reverting. Every observed stream's placement is now recorded, so a later manual move is detected and persisted. Also added LibreWolf, Tor Browser, Waterfox, Floorp, Mullvad Browser, Thorium and ungoogled-chromium to the browsers auto-routed to the Media channel. GUI crash when dragging the Smart Volume → Level slider quickly (#63) — the apply worker (a QThread) could be destroyed while still running during rapid re-applies, aborting the application. Worker cleanup now happens only after the thread has fully stopped. Spatial Audio (and the microphone noise-canceling / noise-gate / compressor) toggles did not save their OFF state (#62) — a PySide6 quirk (bool(Qt.CheckState.Unchecked) is True) meant turning these off was never recorded, so e.g. Spatial Audio OFF was lost when saving a custom preset. The OFF state is now persisted correctly.</p>
      </description>
    </release>
    <release version="1.1.46" date="2026-05-30">
      <description>
        <p>Discord (and other apps) lost their audio sink when changing a Sonar profile — applying a Sonar profile or EQ used to restart the whole PipeWire stack, tearing down every node. Apps that do not re-enumerate their output devices when the PulseAudio server connection drops (Discord/Electron in particular) ended up silent until restarted. Profile and EQ changes now restart only the filter-chain service and recreate the virtual-sink loopbacks dynamically, leaving pipewire/pipewire-pulse untouched, so connected apps keep their sink and audio. Virtual sinks showed a generic "pw-loopback-NNN" name in application output pickers and mixers — the Arctis Game/Chat/Media sinks now expose their proper "Arctis … Game/Chat/Media" name and can be selected as an output device (e.g. as Discord's Chat output). Discord appeared as "WEBRTC VoiceEngine" in a channel's application list — applications that report a generic audio-engine name now fall back to their process binary, so Discord shows as "Discord".</p>
      </description>
    </release>
    <release version="1.1.45" date="2026-05-29">
      <description>
        <p>Arctis 7X: daemon crashed with "Failed to find command interface endpoint" — some Arctis 7X (and other Arctis 1 family) firmwares expose the vendor HID command interface with an interrupt IN endpoint only, no OUT endpoint. get_command_endpoint_address() raised an exception instead of falling back, crashing the whole daemon before the GUI could appear. ASM now detects the missing OUT endpoint and routes commands over HID SET_REPORT (control transfer) automatically, which is the correct path for such devices (issue #59).</p>
      </description>
    </release>
    <release version="1.1.44" date="2026-05-29">
      <description>
        <p>dinit (Artix): Sonar mode toggle crashed — switching Game/Chat ↔ Sonar called systemctl --user restart … with no dinit branch, raising FileNotFoundError: systemctl on systems without systemd. The toggle now works on dinit (issue #25). dinit (Artix): EQ / Sonar changes had no effect — applying a new equalizer mode ran dinitctl start pipewire-filter-chain, but start is a no-op when the service is already running, so the freshly generated config was never reloaded (systemd correctly used restart). All config-reload paths now use restart on both init systems — this is the root cause of the "EQ does nothing" reports on dinit (issue #25). Centralised init-system handling — introduced service_control.py, a single abstraction over systemctl --user and dinitctl. The logical→real service-name mapping (notably filter-chain → pipewire-filter-chain on dinit) and the start/restart distinction now live in one place instead of being copy-pasted across ~25 call sites in 11 files. Service calls no longer crash when the init manager binary is absent; they log and skip. This removes ~230 lines of duplicated branching and makes the issue #25 class of bugs impossible to reintroduce per-site. Added a regression test suite (tests/test_service_control.py).</p>
      </description>
    </release>
    <release version="1.1.43" date="2026-05-28">
      <description>
        <p>Distrobox container fails to start after reboot on Bazzite / SteamOS / Silverblue — /run/asm-hidraw lives on a tmpfs and is wiped at every boot; the sudo mkdir -p in the install script only ran at container creation time, so on the next boot crun could not stat the bind-mount source and refused to start the container (crun: cannot stat /run/asm-hidraw: No such file or directory). A systemd-tmpfiles drop-in (/etc/tmpfiles.d/asm-hidraw.conf) is now installed on the host, recreating the directory at sysinit.target well before Distrobox starts. Added ConditionPathIsDirectory=/run/asm-hidraw to arctis-manager.service to prevent crash-loops if the directory is somehow absent. Affects all three Distrobox installers (bazzite.sh, steamos.sh, silverblue.sh — issue #59).</p>
      </description>
    </release>
    <release version="1.1.34" date="2026-05-18">
      <description>
        <p>Wireless headset disconnect/reconnect loop — when the USB dongle stays plugged in but the RF link drops (headset powered off or out of range), ASM retried USB commands thousands of times per second with no back-off, preventing the dongle firmware from completing RF re-association. Errors with errno 19 (ENODEV) now trigger a 1-second back-off; after ~10 s of consecutive failures ASM voluntarily releases the handle to let the dongle reconnect cleanly. Removed a redundant request_device_status() call that was firing on every loop iteration instead of the intended 2-second cadence (issue #49).</p>
      </description>
    </release>
    <release version="1.1.33" date="2026-05-18">
      <description>
        <p>ClearCast crash on startup when noise cancellation was enabled — _NoiseCancelingCard connected the toggle signal before creating self._slider; PySide6 fires checkStateChanged synchronously during setChecked(), so restoring an enabled state from the previous session triggered _set_enabled before the slider existed, raising AttributeError (issue #51). README: First launch section — clarified that the daemon starts automatically but the GUI must be opened separately (asm-gui or via the app launcher), documented --systray mode and per-DE autostart methods.</p>
      </description>
    </release>
    <release version="1.1.32" date="2026-05-18">
      <description>
        <p>Audio redirect broken for 8 wireless models — is_device_online() compared on_off parser values ('on'/'off') against YAML online_value: online literally; added aliasing so 'on' ↔ 'online' and 'off' ↔ 'offline' are treated as equivalent (discussion #48). Streams stay on dead loopbacks after disconnect — redirect_audio() changed the default sink but did not migrate active streams; now iterates sink_input_list() and moves every stream sitting on an ASM-owned sink, and persists the choice via pw-metadata for PipeWire restart survival (issue #50). babel missing from packaging metadata — added to PKGBUILD depends, RPM Requires, debian/control Depends, system deps checker, and packaging drift check.</p>
      </description>
    </release>
    <release version="1.1.31" date="2026-05-16">
      <description>
        <p>Microphone routed to wrong device on startup — when the Arctis headset is not yet detected when the filter-chain config is written, target.object was left empty and PipeWire would bind the virtual microphone to the first available source (e.g. a DualSense controller mic). check_and_fix_stale_configs() now detects this condition at daemon startup and patches the target in-place, triggering a filter-chain restart.</p>
      </description>
    </release>
    <release version="1.1.30" date="2026-05-15">
      <description>
        <p>Per-channel output device selection — each audio channel card (Game / Chat / Media) now has a dropdown to route that channel to a specific output sink; systray → Output Routing mirrors the same selection (issue #46). Output device routing saved in profiles — optional checkbox in Save Profile dialog captures the current per-channel routing. Language filter — only languages with ≥ 80 % translation coverage appear in the language selector; names are now displayed in their native script via babel (Français, Deutsch, Polski…) instead of 2-letter codes.</p>
      </description>
    </release>
    <release version="1.1.4" date="2026-05-08">
      <description>
        <p>dinit: filter-chain and video-router stuck stopped after asm-setup — when asm-setup rewrites service files on disk, dinit keeps the old in-memory definition (without restart = true) until explicitly reloaded. Added dinitctl reload for stopped services before starting them so dinit always picks up the current file (issue #25). dinit: D-Bus collision on re-login — the guard preventing a second asm-daemon from starting used dinitctl status, which could miss the race window between dinit's boot-sequence auto-start and asm-setup's explicit start. Replaced with pgrep -f asm-daemon against the actual running process. dinit: filter-chain start race after pipewire restart — asm-setup now waits 0.5 s after a successful dinitctl restart pipewire before starting dependants, giving the PipeWire socket time to come up.</p>
      </description>
    </release>
    <release version="1.0.99" date="2026-05-06">
      <description>
        <p>Adaptive multi-DE autostart — Hyprland, Sway, XDG, systemd, dinit — the "Launch at startup" toggle now automatically selects the right method for the detected desktop environment / init system. On Hyprland it writes exec-once = asm-gui --systray into ~/.config/hypr/hyprland.conf (using a comment marker for clean enable/disable). On Sway it writes exec asm-gui --systray into ~/.config/sway/config. On dinit systems the existing dinitctl enable calls are used. On systemd-based DEs (GNOME, KDE, XFCE, …) the arctis-gui.service approach is kept. Any other setup (i3, unknown, non-systemd distros) falls back to an XDG .desktop file in ~/.config/autostart/. Switching DEs cleans up stale entries from the previous method. Fixes the issue reported by users running Hyprland/CachyOS where the toggle had no effect (closes #30).</p>
      </description>
    </release>
    <release version="1.0.98" date="2026-05-05">
      <description>
        <p>Distrobox hot-plug (issue #26) — the headset can now be plugged in *after* the container is created without requiring --reinstall. A new udev rule (90-asm-hidraw-symlink.rules) maintains /run/asm-hidraw/ as a stable directory of symlinks to SteelSeries hidraw nodes; the container mounts this directory with rslave propagation so additions and removals on the host are immediately visible inside. libusb / PyUSB access from container — /dev/bus/usb is now bind-mounted into the container (rslave), giving PyUSB full access to the USB device tree. Previously only /dev/hidraw* was passed, which blocked the libusb backend entirely. Fedora Silverblue/Kinoite variants auto-detected — distrobox-install.sh now correctly routes Bluefin, Aurora, Sericea, Onyx, and Cosmic Atomic to silverblue.sh (Fedora COPR container) instead of incorrectly using the Arch/AUR container.</p>
      </description>
    </release>
    <release version="1.0.97" date="2026-05-05">
      <description>
        <p>Sonar Media tab (issue #29) — new Media EQ tab placed between Game and Chat in the Sonar page. The channel runs through the HeSuVi 7.1 surround pipeline (same routing as Game), giving music and video apps the full spatial-audio treatment. The complete 300+ preset library ([Game] tag — games, music genres, movies, podcasts) is available from day one. Boost Volume and Smart Volume cards are wired up identically to Game and Chat. Arctis_Media → sonar-media-eq — the Media virtual sink now routes through its own dedicated EQ filter-chain node instead of bypassing Sonar entirely. Stream restore after filter-chain restart remaps effect_input.sonar-media-eq ↔ Arctis_Media correctly.</p>
      </description>
    </release>
    <release version="1.0.96" date="2026-05-05">
      <description>
        <p>dinit support (Artix Linux / issue #25) — ASM now works end-to-end on init systems other than systemd. asm-setup detects dinit via /proc/1/comm and writes service files to ~/.config/dinit.d/ instead of calling systemctl. All service management calls in the GUI (Sonar EQ restarts, autostart toggle, systray shutdown) are branched: systemd systems are completely unaffected, dinit systems get the correct dinitctl start/stop/enable calls. _ensure_dinit_boot_target() — on Artix dinit-userservd there is no default user boot service, so dinitctl enable silently failed ("service 'boot' has no waits-for.d directory"). asm-setup now creates a minimal ~/.config/dinit.d/boot + boot.d/ when absent (never overwrites an existing one), matching the upstream dinit getting-started guide. asm-diag-dinit diagnostic script — reports init system, service file presence, running/enabled state (via waits-for.d symlink inspection), filter-chain, virtual sinks, udev rules and D-Bus in a single read-only pass. Useful for triage on any non-systemd distro.</p>
      </description>
    </release>
    <release version="1.0.95" date="2026-05-04">
      <description>
        <p>OLED horizontal marquee for EQ preset name — when the EQ preset name overflows the 128 px display width, the text now scrolls left pixel-by-pixel. It pauses 2 s at the start (readability), scrolls to the end, pauses 2 s, then snaps back. Speed is controlled by the new Scroll Speed (Horizontal) slider (0 = disabled, 1–5 = slow to fast). OLED horizontal marquee for Profile name — same marquee behaviour applied to the active profile name line. DAC tab — Scroll Speed (Horizontal) slider — new oled_eq_scroll_speed setting controls the horizontal marquee speed for both EQ preset and profile name lines. The existing scroll slider is renamed to Scroll Speed (Vertical).</p>
      </description>
    </release>
    <release version="1.0.94" date="2026-05-04">
      <description>
        <p>Arctis 7 2019 (0x12ad) failed to initialize — no battery, no chatmix (issue #28) — arctis_7.yaml was missing command_transport: ctrl_output, the field that tells the HID layer to use SET_REPORT on the control endpoint. All four product IDs sharing that YAML (0x1260, 0x12ad, 0x1252, 0x1280) were affected. Adding ctrl_output aligns the Arctis 7 family with every other device YAML in the codebase (Arctis 7+, Arctis 9, Nova 3/5/7). Virtual sinks (Arctis_Game / Arctis_Chat / Arctis_Media) routed to system default instead of Sonar EQ nodes (issue #28) — the bundled 10-arctis-virtual-sinks.conf installed by install.sh / asm-setup had no node.target in its playback.props blocks. PipeWire therefore routed all three loopback outputs to the default sink, bypassing the Sonar EQ filter-chain entirely and breaking chatmix separation. Each sink now points to its Sonar EQ input node (effect_input.sonar-game-eq / effect_input.sonar-chat-eq). Virtual Surround sink silent on fresh install — can't start graph: No such file or directory (issue #28) — sink-virtual-surround-7.1-hesuvi.conf referenced the HRIR WAV as the relative path hrir_hesuvi/hrir.wav. PipeWire instantiates filter-chains without a stable working directory, so the file was never found. The template now uses the placeholder ${HRIR_DIR}, which install.sh, setup-surround.sh, and asm-setup substitute with the absolute path ~/.local/share/pipewire/hrir_hesuvi at deploy time — the same directory the installer downloads the WAV to.</p>
      </description>
    </release>
    <release version="1.0.86" date="2026-04-30">
      <description>
        <p>System Deps dialog at GUI startup — a runtime self-healing dialog that detects every missing system component ASM relies on (LADSPA plate_1423 + librnnoise, HRIR file, filter-chain.service, PipeWire ≥ 1.0, wpctl, pkexec, pyudev/pulsectl/PySide6/pyusb/PIL/ruamel.yaml/dbus-next, dbus-send, pw-metadata, curl, D-Bus session, udev rules, gh CLI). Each missing dep shows a severity badge (BLOCKING / DEGRADED / OPTIONAL), what feature breaks if it stays missing, and an Install/Run button that runs the right pkexec dnf|apt-get|pacman install … for the detected distro. Install all missing groups by package manager so a single polkit prompt fixes the whole list. Don't show again until ASM is upgraded writes a version-aware skip marker that auto-resets on the next ASM upgrade. Copy cmd falls back to clipboard mode for unsupported distros. Triggered after the existing udev / telemetry dialogs at T+2.5 s on first launch — no nag. asm-daemon --verify-setup runs the same dep registry — headless preflight now logs each missing component with its severity tier and per-distro install command, plus the detected distro on the summary line. Exit 1 on BLOCKING/DEGRADED, 0 if only OPTIONAL — safe to wire into systemd ExecStartPre. scripts/check-deps-drift.py CI guard — AST-walks src/, fails the wheel-install-test matrix on every supported distro if a new third-party import or subprocess.run([…]) is added without a matching DepCheck entry. Closes the silent-failure trap class issue #23 was about: any new external dep is forced through the runtime checker too, so the GUI dialog never goes blind.</p>
      </description>
    </release>
    <release version="1.0.85" date="2026-04-27">
      <description>
        <p>Sonar Output preset switch dropped Firefox / Arctis_Media routing (issue #22) — restarting the PipeWire filter-chain to apply a new Output EQ preset (e.g. *Music – Punchy*) tears down the Arctis_Game/Chat/Media virtual sinks momentarily. The stream-restore loop only waited for the EQ filter node to reappear before re-issuing pactl move-sink-input, so when the Arctis virtual sinks were not yet back the moves silently failed and streams stayed orphaned on the system default. The apply worker now waits up to 4 s per saved Arctis_* target sink before issuing the move-back commands. asm-cli arctis-usb-info traceback on EACCES (issue #22) — when udev rules were missing or not yet applied to the currently-attached dongle, reading device.manufacturer raised usb.core.USBError: [Errno 13] Access denied and aborted the diagnostic. Now wrapped with try/except; prints (no permission) with a hint to run asm-setup instead of crashing the whole CLI report.</p>
      </description>
    </release>
    <release version="1.0.84" date="2026-04-27">
      <description>
        <p>TypeError crash in on_settings_received when the daemon flagged a USB EACCES (issue #22) — UdevRulesDialog(parent=self, ...) was called from QMainApp, which inherits from QObject, not QWidget, so QDialog.__init__ rejected the parent and the GUI tracebacked instead of opening the "Apply now" dialog. Pass self.main_window (the actual QMainWindow) so the runtime fix-permissions flow shipped in v1.0.81 finally fires on Nobara/Fedora setups where the dongle was plugged in before the udev rules took effect.</p>
      </description>
    </release>
    <release version="1.0.83" date="2026-04-26">
      <description>
        <p>Bug report — full system diagnostic — the report payload now includes Python library versions (pulsectl / pyudev / pyusb / dbus-next / ruamel-yaml / PySide6 / pillow via importlib.metadata), multi-install detection (rpm + pacman + apt + pipx + every asm-daemon binary in $PATH), the actual content of the active udev rules file with is_udev_rules_valid()'s verdict, the full PipeWire sink list, wpctl status, the USB monitor backend (pyudev event-driven vs polling fallback) and the D-Bus session bus path. Goes from a 6 kB report to a ~13 kB one — every block is actionable for triage. Bug report — short URL body + full file attachment — GitHub's ?body= query param is capped around 8 kB; the expanded report no longer fits. ASM now writes the full diagnostic to ~/.cache/arctis-sound-manager/reports/bug-report-YYYYMMDD-HHMMSS.md, opens the GitHub issue editor with a short summary (~600 chars) and an "Open folder" button so the user can drag-and-drop the diagnostic file into the issue editor. Bug report — one-click auto-submit via gh CLI — when the user has gh auth status configured, a "Submit automatically (gh CLI)" button uploads the diagnostic as a secret gist, creates the issue with a link to the gist, and opens the new issue URL in the browser. Zero clicks past the dialog. Falls back to the manual drag-and-drop flow on any failure.</p>
      </description>
    </release>
    <release version="1.0.82" date="2026-04-26">
      <description>
        <p>Multi-device routing (issue #20) — on systems with several audio outputs (Logitech G560, Razer Kiyo, internal speakers, etc.) plus an Arctis headset, apps that were already running when ASM started used to stay glued to their previous sink because _auto_route() only matched a hardcoded list of browser/game/chat app names. Now video_router.py adopts any orphan stream onto Arctis_Media when Arctis is the default sink — Spotify, VLC, Steam and friends finally end up in the headset on first launch. Streams already on an Arctis sink (virtual or filter-chain) are left untouched, and the adoption is saved as a routing override so manual KDE-mixer placements take over from there. Same logic applied to native PipeWire streams (mpv, haruna).</p>
      </description>
    </release>
    <release version="1.0.81" date="2026-04-26">
      <description>
        <p>Runtime "Apply now" popup on USB EACCES — restores the v1.0.71 behaviour lost during the v1.0.79 merge. The existing startup dialog only fires when the udev rules file is missing/incomplete on disk, but if the rules are correct AND the headset was plugged in before they took effect (typical after a paru -Syu or dnf upgrade), the daemon used to hit EACCES on kernel_detach and the GUI was silent. Users had to figure out replug or sudo asm-cli udev reload-rules on their own. Now CoreEngine.kernel_detach flags permission_error=True per-interface on errno 13, the daemon exposes that flag through GetSettings, and the GUI opens UdevRulesDialog(mode="reload") automatically — one click runs asm-cli udev reload-rules with a single pkexec prompt and the device becomes accessible without unplugging. UdevRulesDialog two modes: write (existing — install missing rules) and reload (new — re-trigger udev on the currently-attached device). Both share the same one-prompt elevation flow. debian/build-deb.sh referenced stale unit paths (debian/*.service) that had moved to systemd/*.service in v1.0.79. The .deb job in release.yaml failed at build, which cascade-skipped aur-update and copr-build, leaving AUR / COPR users on v1.0.78 even though the v1.0.79 / v1.0.80 tags were pushed. Updated to use the canonical systemd/ paths.</p>
      </description>
    </release>
    <release version="1.0.80" date="2026-04-26">
      <description>
        <p>scripts/uninstall.sh — dedicated uninstaller. Detects every install method on the system (rpm / pacman / apt / pipx) and lets the user pick which one(s) to remove. When both pipx and a distro package coexist, offers a 1/2/3 menu (pipx only / distro only / both). --purge wipes settings, PipeWire configs, HRIR data, user systemd units and the manually-written udev rules in /etc, but preserves audio profiles (~/.config/arctis_manager/profiles/ and the active-profile pointer) so a future reinstall picks them right back up. A separate explicit confirm offers to delete the profiles too. Profiles capture DAC tab settings — audio profiles now snapshot and restore the full DAC state in addition to EQ/macros/spatial/volumes: OLED brightness, screen timeout, scroll speed, custom-display toggle, show-element toggles, display order, per-element font sizes, weather configuration. Older profile files (pre-v1.0.80) load with an empty DAC block — restoring them is a no-op for the DAC side, exactly as before. scripts/check-packaging-drift.py — CI-enforced drift detector. Runs on every PR/push across the 8-distro matrix and fails the build if pyproject.toml / PKGBUILD / .SRCINFO / RPM spec are out of sync, if any generator (udev rules, AppStream metainfo, debian/changelog) produces output that differs from the committed file, or if a pyproject dep is missing from PKGBUILD depends, the spec's Requires: lines, or debian/control Depends:. Closes the class of bug that left AppStream stuck at v1.0.4 and debian/changelog at v1.0.27 for months.</p>
      </description>
    </release>
  </releases>

  <categories>
    <category>Audio</category>
    <category>Utility</category>
  </categories>

  <provides>
    <binary>asm-gui</binary>
  </provides>
</component>
