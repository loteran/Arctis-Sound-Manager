# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
debian/wheels/
*.egg-info

# Virtual environments
.venv
aur/src/
aur/pkg/
aur/*.tar.gz
aur/*.tar.zst
aur/*.pkg.tar.*

# Cloudflare / telemetry secrets (never commit these)
cloudflare/.dev.vars
cloudflare/.mf/
cloudflare/node_modules/
.wrangler/

# Local dev notes (personal, not versioned)
NOTES.md

# Claude Code context (not versioned)
CLAUDE.md
